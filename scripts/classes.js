function ProductItems(name, details, img, price, stock, properties, id) {
    this.name = name;
    this.details = details;
    this.img = img;
    this.price = price;
    this.stock = stock;
    this.properties = properties;
    this.id = id;

}

function Cart() {
    this.products = [];

    //make a function that adds products into cart
    //make a function that removes products into cart
}